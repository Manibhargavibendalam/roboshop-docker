services:
  mongodb:
    image: mongodb:v1
    container_name: mongodb
  catalogue:
    image: catalogue:v1
    container_name: catalogue

networks:
  default:
    driver: bridge
    name: roboshop
    external: false # true if you have created 